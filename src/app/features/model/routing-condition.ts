export interface IRoutingConditionContent {
  stt: number; // thuộc tính cho stt ở bảng hiển thị trên UI
  id: string;
  flowName: string;
  condCode: string;
  condName: string;
  condRank: string;
  active: boolean;
  channelType: string;
  transFlow: string;
}
