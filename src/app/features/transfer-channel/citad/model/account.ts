export interface IAccountContent {
    stt?: number; // thuộc tính cho stt ở bảng hiển thị trên UI
    id: string;
    accountNo: string;
    reason: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
  }